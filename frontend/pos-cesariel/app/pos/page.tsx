"use client";

import { POSContainer } from "@/features/pos";

/**
 * Point of Sale Page
 *
 * Main sales interface with product selection, cart management,
 * barcode scanning, and checkout processing.
 */
export default function POSPage() {
  return <POSContainer />;
}
