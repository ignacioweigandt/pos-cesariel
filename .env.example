# =================================
# Environment Configuration Template
# POS Cesariel - Development & Production
# =================================
#
# IMPORTANT: This is a template file. Copy to .env and fill with real values.
#
# Setup:
#   cp .env.example .env
#   nano .env  # Edit with your values
#
# DO NOT commit .env to git (it contains secrets)

# =================================
# ENVIRONMENT
# =================================
ENV=development  # Options: development, production, staging
DEBUG=true       # Set to false in production

# =================================
# DATABASE (PostgreSQL)
# =================================
DB_HOST=db                    # Use 'db' for Docker, 'localhost' for local
DB_PORT=5432
DB_NAME=pos_cesariel
DB_USER=postgres
DB_PASSWORD=your_secure_password_here  # CHANGE THIS!

# Constructed DATABASE_URL (don't modify, auto-built from above)
# DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# =================================
# JWT AUTHENTICATION
# =================================
# Generate a secure secret key:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your_super_secret_jwt_key_at_least_32_chars_long
JWT_EXPIRE_MINUTES=480  # 8 hours

# =================================
# CLOUDINARY (Image Storage)
# =================================
# Get from: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key_here
CLOUDINARY_API_SECRET=your_api_secret_here

# =================================
# FRONTEND URLS
# =================================
# POS Admin Frontend
NEXT_PUBLIC_API_URL=http://localhost:8000

# E-commerce Frontend
# Server-side (Docker internal)
API_URL=http://backend:8000
# Client-side (browser)
NEXT_PUBLIC_API_URL=http://localhost:8000

# =================================
# OPTIONAL: EXTERNAL SERVICES
# =================================
# Sentry Error Tracking (optional)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Redis Cache (optional, for future use)
# REDIS_URL=redis://localhost:6379/0

# =================================
# DEVELOPMENT SETTINGS
# =================================
# Next.js file watching (for Docker on Mac/Windows)
WATCHPACK_POLLING=true
CHOKIDAR_USEPOLLING=true

# =================================
# PRODUCTION SETTINGS (when ENV=production)
# =================================
# Use strong passwords
# Enable HTTPS
# Set DEBUG=false
# Regenerate all secrets
# Use managed database service
# Set up backups
# Configure CDN for static assets
